#include <bits/stdc++.h>
#define int long long
#define endl "\n" 
#define pii pair<int,int>
using namespace std;
void solve(){
    set<int> s;
    for (int i = 0; i < 5; i++)
    {   int x;
        cin>>x;
        s.insert(x);
        for (auto it = 0; i < count; i++)
        {
            /* code */
        }
        
    }
    
}
int_32t main(){
  ios::sync_with_stdio(0);
  cin.tie(nullptr);
  cout.tie(nullptr);
  int t=1;
  cin >> t;
  while (t--){
    solve();
   }
    return 0;
}